# 11 不具合診断表

最初に `git status`、Rojo接続先、Script Analysis、Server/Client Outputを確認します。一度に一仮説だけ検証してください。

| 症状 | 主な原因 | 確認場所 | 診断方法 | 安全な修正候補 | 触らない場所 |
| --- | --- | --- | --- | --- | --- |
| Characterが出ない | Spawn、CharacterAutoLoads、Server error | Explorer/Server Output | CourseSpawn、Players、CharacterAdded log | timeoutとfallbackを局所修正 | Course座標 |
| 歩けない | Controls停止、WalkSpeed 0、Humanoid state | Client Output/Properties | writer検索、終了処理確認 | controlsとHumanoid値を復元 | MapBuilder |
| 勝手に前進する | Controller/legacyの速度writer | 全src/Client Output | velocity、Force、Moveを検索 | 主制御を一つだけ停止 | Road位置 |
| 坂で加速しない | 摩擦、方向、Humanoid相殺、ownership | Physics/Probe | 接線重力、速度時系列、owner | 一要因ずつ比較 | slope座標 |
| 平地で止まらない | 摩擦不足、常時Force、速度上書き | Properties/Client | flat区間でForceと速度writer確認 | 補助力停止、減衰を別工程追加 | Course形状 |
| 上りでも加速する | slope符号、固定target speed | Controller log | forwardとgravity投影dot | 上り時補助を禁止 | Road生成順 |
| 左右入力が効かない | gameProcessed、Controls競合、right逆 | Client | input値とtrackRightを別log | action bindを局所修正 | StartPad |
| ジャンプできない | jump state無効、JumpPower 0 | Humanoid/Client | JumpRequestと接地を確認 | Phase 2工程6で一方式追加 | コブ位置 |
| 空中で暴れる | Ground Stick継続、強いorientation | Physics/Client | grounded遷移とangular velocity | 空中時に地面制御を切る | Course |
| 着地で跳ねる | Elasticity、下向き補正、BodyPart衝突 | Properties/Physics | 法線速度と接触Partを記録 | elasticity/補正を一つずつ調整 | jump形状 |
| 壁に張り付く | 力が壁法線へ向く、摩擦 | Raycast/Physics | 床と壁normalを区別 | lateral成分とwall cooldown | 壁位置 |
| コースから落ちる | steering過大、gap、復帰未実装 | Studio/Map診断 | Road hit、index、last safeを記録 | 復帰は別工程で追加 | Road座標 |
| Baseplateに衝突する | 退避未実行、Studio旧Baseplate | Workspace/Output | Yと4 property、同名数を確認 | Rojo/Play状態を整合 | 退避処理 |
| CameraがCharacterを見失う | 古いCharacter参照、Subject未復元 | Client/Camera | respawn前後のSubjectをlog | CharacterAddedで再bind | physics |
| Cameraがリセットされる | 別camera script、Character respawn | Client search | CameraType writer検索 | ownershipをcamera script一つへ | player movement |
| Rojo同期されない | wrong folder/port/project | Rojo plugin/terminal | project名、34872、tree比較 | 正しいrootでserve再開 | Studio内容の削除 |
| Studioの変更が消える | Rojo管理対象をStudioで編集 | project tree/Git | `$path` とInstance path比較 | local sourceへ移植して同期 | 未保存place |
| Script Analysis error | 型、nil、syntax | Script Analysis | 最初のerrorから修正 | 対象行の最小修正 | 無関係file |
| Output error | runtime nil、timeout、重複起動 | Server/Client Output | stack traceとprefixを確認 | 原因moduleだけ停止・修正 | 全体書換え |
| Clientでは動くがServerでは動かない | ownership、Remote、replication | Server & Clients | owner、Remote方向、server state | Server検証とauthority整理 | force owner変更 |
| Gitに変更が出ない | 別folder、Studio-only edit、ignored | terminal | root、`git rev-parse`、status | 正しいlocal fileを編集 | reset |
| 意図しないfileが変更される | formatter、Rojo生成物、改行変換 | `git diff --name-only` | fileごとにdiff | 対象だけstage、原因を止める | 他者変更 |

## 診断記録テンプレート

```text
症状:
再現手順:
期待:
実際:
Server Output:
Client Output:
確認したwriter:
仮説:
一つの比較変更:
結果:
次の判断:
```

